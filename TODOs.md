# OpenList TMDB 注入版 TODOs

## 已完成

- [x] 拆分注入片段为外链加载的 CSS 和 JS 两份：
  - `head.css`
  - `body.js`
- [x] 在右下角工具栏插入 TMDB 匹配入口。
- [x] 新增左侧悬浮提示，匹配 OpenList 右下角工具栏交互。
- [x] 支持在浏览器 `localStorage.openlist_tmdb_api_key` 保存 TMDB API Key。
- [x] 支持 TMDB v3 `api_key` 和 v4 Bearer token 鉴权判断。
- [x] 读取当前目录视频文件列表。
- [x] 根据目录和文件名自动默认电影 / 电视剧模式。
- [x] 支持手动切换电影 / 电视剧模式。
- [x] 支持单个电影搜索、选择、改名和 movie NFO 上传。
- [x] 支持单集电视剧季集解析、剧集搜索、改名和 episode NFO 上传。
- [x] 执行成功后保持模态框打开，刷新列表并自动选中下一项。
- [x] 验证 PC 和移动端基础交互可用。

## 高优先级

- [x] 电视剧多集批量确认：
  - [x] 一次选择当前目录内同一部剧的多集文件。
  - [x] 批量解析 S/E 信息。
  - [x] 批量拉取 TMDB episode 信息。
  - [x] 显示逐集预览表。
  - [x] 对解析失败项支持手动填写季 / 集。
  - [x] 用户确认后逐条执行改名和 NFO 上传。
- [x] 元数据完整性检查：
  - [x] 扫描当前目录视频文件。
  - [x] 标记缺失 `.nfo` 的视频。
  - [x] 标记缺失 `.jpg` / poster 的视频。
  - [x] 标记命名不规范或季集解析失败的文件。
  - [x] 提供只读检查报告，不自动修改文件。

## 中优先级

- [x] 封面 / 缩略图下载：
  - [x] 电影支持写入 `poster.jpg` 或 `电影名 (年份)-poster.jpg`。
  - [x] 电视剧单集支持写入同 basename 的 `.jpg`。
  - [x] 电视剧目录支持写入 `poster.jpg` 或 `folder.jpg`。
  - [x] 使用 `/api/fs/put` 上传图片文件。
- [ ] 文件名清理工具：
  - [ ] 批量去除资源站标签、广告词和无关括号内容。
  - [ ] 可选去除 `1080p`、`WEB-DL`、`H.264`、`AAC` 等技术标签。
  - [ ] 提供改名前预览。
  - [ ] 使用 `/api/fs/batch_rename` 执行。
- [ ] 同目录重复 / 疑似重复检测：
  - [ ] 按标准化标题检测重复电影。
  - [ ] 按剧名、季、集检测重复剧集。
  - [ ] 按文件大小接近度辅助判断。
  - [ ] 只提示，不自动删除。

## 低优先级

- [ ] STRM 生成器：
  - [ ] 为选中文件批量生成 `.strm`。
  - [ ] 支持写入 `/d/...` 地址。
  - [ ] 支持写入当前文件直链。
- [ ] NFO 快速编辑器：
  - [ ] 列出当前目录已有 `.nfo` 文件。
  - [ ] 在模态框内编辑 XML 文本。
  - [ ] 保存时通过 `/api/fs/put` 覆盖写入。
- [ ] 更平滑的页面刷新：
  - [ ] 研究是否能触发 OpenList 前端内部刷新事件。
  - [ ] 避免需要整页刷新或仅依赖重新调用 `/api/fs/list`。

## 已知限制

- 注入版仍依赖 OpenList 前端 DOM 结构，升级前端后可能需要调整按钮插入选择器。
- 批量操作没有事务，可能出现部分成功、部分失败。
- TMDB API 和图片由浏览器直接访问，可能受网络环境影响。
- TMDB API Key 存在浏览器 localStorage，不是服务端全局配置。
