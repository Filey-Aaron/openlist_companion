# OpenList TMDB 影视元数据助手 TODO

## 项目假设

本项目面向开发者本人维护的可信、自建 OpenList 站点，以轻量、直接、易维护为优先目标。

- 接受在浏览器 `localStorage` 中保存 TMDB API Key 和用户偏好。
- 接受注入脚本读取 OpenList 已有登录 token，以调用文件 API。
- 接受浏览器直接访问 TMDB API 和图片域名，近期不引入后端代理。
- 电视剧批处理继续限定为当前目录，并统一使用一个 TMDB 电视剧条目。
- 继续采用原生 JavaScript / CSS 外链注入，不引入构建系统或前端框架。
- 当前重点是数据一致性、失败恢复、页面兼容性和网络稳定性，而不是面向不受信任用户的安全加固。

## 已有能力

- [x] 通过 `head.css` 和 `body.js` 注入 OpenList 普通目录页面。
- [x] 在右下角工具栏插入 TMDB 入口，并提供桌面端和移动端界面。
- [x] 读取当前目录的视频文件、目录写权限和 OpenList 配置。
- [x] 支持 TMDB v3 API Key 与 v4 Bearer Token。
- [x] 支持电影和电视剧搜索、详情选择及命名预览。
- [x] 解析电影年份和常见电视剧季集格式。
- [x] 支持单个电影、单集电视剧的改名、NFO 和图片写入。
- [x] 支持同目录电视剧多集选择、季集修正和逐集预览。
- [x] 以 5 路并发拉取批量剧集信息。
- [x] 使用 `/api/fs/batch_rename` 一次提交批量改名。
- [x] 支持电影海报、电视剧目录海报和单集缩略图。
- [x] 提供只读元数据检查，报告缺失 NFO、图片和命名问题。
- [x] 执行成功后刷新文件列表，并尽量自动选择下一项。

## P0：数据一致性与失败恢复

### 执行前检查

- [x] 在真正写入前生成完整执行计划，列出每个视频的改名、NFO 和图片目标。
- [x] 同时检查视频目标名、NFO、单集图片和目录海报是否已经存在。
- [x] 将“新建”“覆盖”“无需变更”“冲突”明确显示在预览中。
- [x] 默认不静默覆盖已有 NFO 或图片；发现覆盖项时要求用户明确确认。
- [x] 执行任何文件变更前，先完成所需 TMDB 详情和图片的下载或可用性检查。

### 可恢复执行

- [x] 为每个文件分别记录改名、NFO、单集图片和目录海报的执行状态。
- [x] 失败后保留已经完成的步骤，重试时只执行未完成或失败的步骤。
- [x] 单文件在改名后发生错误时，立即重新读取目录并定位实际的新文件名。
- [x] 批量失败项使用改名后的真实文件名重新建立选择状态。
- [x] 将电视剧目录海报作为独立步骤；海报失败不得阻断逐集 NFO 和缩略图。
- [x] 操作结束后给出成功、失败、跳过和覆盖数量摘要。
- [x] 增加可复制的失败明细，至少包含文件名、失败步骤和 API 错误。

### 冲突与覆盖保护

- [x] 单文件流程补齐与批量流程一致的目标文件名冲突检查。
- [x] 区分“目标已存在”和“当前文件无需改名”。
- [x] 对已有手工 NFO 提供跳过、覆盖两种策略，默认选择跳过。
- [x] 避免因重试重复改名、重复上传或错误覆盖其他文件。
- [x] 已评估 NFO / 图片备份：当前以默认跳过和显式覆盖保护为主，近期不实现备份、完整事务或服务端回滚。

## P1：OpenList 页面兼容性

### 工具栏入口生命周期

- [x] 修复工具栏首次成功插入后 `MutationObserver` 永久断开，导致后续 DOM 替换无法恢复入口的问题。
- [x] 同时处理 `pushState`、`replaceState`、`popstate` 和工具栏节点被替换的情况。
- [x] 页面切换后确认按钮只存在一个，不重复注册事件。
- [x] 避免长期观察整个 `documentElement` 带来的无意义回调，只观察必要的应用根节点或按路由重新连接。
- [x] 在找不到兼容工具栏时输出可见且可诊断的提示。

### 目录与状态同步

- [x] 每次打开模态框和执行写入前验证 URL 推导出的目录与已加载的 OpenList 目录一致。
- [x] 目录切换后清理不再存在的文件选择、TMDB 条目和批量行状态，并丢弃晚返回的旧目录请求。
- [ ] 操作完成后优先触发 OpenList 内部目录刷新；无法触发时继续使用 `/api/fs/list` 回读。
- [ ] 检查 OpenList 升级后 `OPENLIST_CONFIG`、toolbar 选择器和文件 API 数据结构是否变化。

## P1：TMDB 请求稳定性

- [ ] 为 TMDB API、图片下载和 OpenList 写请求增加明确的超时。
- [ ] 对 429、502、503、504 实现有限次数的指数退避重试，并遵守 `Retry-After`。
- [ ] 鉴权失败、参数错误和剧集不存在等确定性错误不自动重试。
- [ ] 保持批量剧集请求的并发上限，必要时在遇到 429 后自动降低并发。
- [ ] 按 TMDB ID、语言、季、集缓存本次会话中的详情，避免手动修正和重试时重复请求。
- [ ] 图片上传前校验响应类型，避免把 HTML 错误页作为 JPG 写入。
- [ ] 提供适中的默认图片尺寸，将 `original` 作为可选项。
- [ ] 区分 TMDB API 不可达、图片域名不可达、OpenList 上传失败和请求超时。

## P1：权限判断与错误提示

- [ ] 梳理 OpenList 的目录 `write`、用户 rename 权限、write-content 权限和 `write_content_bypass` 的实际语义。
- [ ] 不再用单一 `state.write` 同时判断改名、NFO 和图片能力。
- [ ] 根据可用权限分别禁用“改名”“生成 NFO”“下载图片”选项并说明原因。
- [ ] 保留服务端 API 错误作为最终判断，前端权限检查只用于提前提示。
- [ ] 无写权限时仍允许使用只读元数据检查和 TMDB 搜索预览。

## P2：批处理正确性与体验

- [ ] 在电视剧批量确认区明确提示：所有选中文件将使用同一个 TMDB 电视剧条目。
- [ ] 执行前显示所选文件数、涉及季数、季集范围和无法解析数量。
- [x] 检测重复的季 / 集组合，防止多个文件生成相同目标名。
- [ ] 对疑似不属于当前剧集的文件给出提示，但不做强制自动判断。
- [ ] 支持按文件顺序批量填充连续集数，供无法从文件名解析时使用。
- [ ] 支持只重试失败项和只执行某一类操作，例如仅补 NFO。
- [ ] 记住用户的覆盖策略、图片尺寸和批量并发偏好。

## P2：文件整理工具

### 文件名清理

- [ ] 批量识别并移除资源站标签、广告词和无关括号内容。
- [ ] 可选移除 `1080p`、`2160p`、`WEB-DL`、`BluRay`、编码和音频等技术标签。
- [ ] 所有清理规则均提供逐项预览和取消选择。
- [ ] 复用统一的冲突检查与可恢复执行流程。

### 重复检测

- [ ] 按标准化电影标题和年份提示疑似重复电影。
- [ ] 按剧名、季、集提示疑似重复剧集。
- [ ] 将文件大小接近度作为辅助信息。
- [ ] 仅报告和辅助选择，不自动删除文件。

## P3：扩展功能候选

- [ ] STRM 生成器：支持当前文件 `/d/...` 地址及可配置直链模板。
- [ ] NFO 快速编辑器：读取、编辑、校验并覆盖现有 XML。
- [ ] 生成 `tvshow.nfo`、季级 NFO 和季封面。
- [ ] 支持电影多文件逐条匹配队列。
- [ ] 支持命名模板、NFO 字段和图片命名策略配置。
- [ ] 支持执行日志导出和导入后继续失败任务。

## 测试与发布

- [ ] 为电影名、季集解析和安全文件名生成建立表驱动测试样例。
- [ ] 为电影 / 单集 NFO 建立 XML 转义和关键字段快照测试。
- [ ] 覆盖目标重名、已有 NFO、图片缺失、改名后上传失败等执行场景。
- [ ] 增加工具栏首次加载、SPA 导航、节点替换和重复注入的浏览器冒烟测试。
- [ ] 建立至少一个受支持 OpenList 版本的手动回归清单。
- [ ] 在脚本中暴露版本号，方便问题反馈时确认实际加载版本。
- [ ] 发布稳定版本时使用 Git tag，并在 README 中推荐固定 tag 的 CDN 地址。
- [ ] 保留 `master` 地址作为主动跟随最新开发版的可选方式。

## 当前不做

- 不为个人可信部署专门建设用户隔离、密钥加密或复杂的安全策略。
- 不把 TMDB API Key 迁移到服务端保存。
- 不在近期实现跨目录、跨电视剧的混合批处理。
- 不在近期修改 OpenList 后端或维护正式 OpenList 前端分支。
- 不为实现理论上的完整事务而引入独立数据库或任务服务。
